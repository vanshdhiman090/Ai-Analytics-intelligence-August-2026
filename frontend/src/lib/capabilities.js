export const CAPABILITIES = Object.freeze([
  Object.freeze({
    id: "root-cause-investigation",
    name: "Root Cause Investigation",
    description: "Trace KPI movements through deterministic contributions, governed evidence, and explicit uncertainty.",
    version: "V1 · Additive KPI",
    status: "active",
  }),
]);

export const CURRENT_CAPABILITY = CAPABILITIES[0];
