# Pinned to the exact versions used and tested during development.
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.141.1
uvicorn==0.52.1
python-multipart==0.0.22

# Agent orchestration
langgraph==1.2.10
langgraph-checkpoint-postgres==3.1.0
psycopg==3.3.4
psycopg-binary==3.3.4
psycopg-pool==3.3.1

# Database
SQLAlchemy==2.0.51
PyMySQL==1.1.1
psycopg2-binary==2.9.12
python-dotenv==1.2.2

# Data processing (Prepare/Process/Analyze stages)
pandas==3.0.2
numpy==2.4.4
openpyxl==3.1.5
python-docx==1.2.0
reportlab==4.4.9

# Visualization (Share stage)
matplotlib==3.10.8

# LLM provider — Gemini (free tier, v1 default per master plan Section 3.5)
# NOTE: google-generativeai is deprecated (confirmed during this install) —
# using the maintained replacement SDK instead.
google-genai==2.16.0

# Testing
pytest==9.0.2
